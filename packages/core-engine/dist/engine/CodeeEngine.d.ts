import { EventEmitter } from "events";
import { EngineLogEntry, EngineTelemetryEvent, ICodeeEngine, Session, StreamResponse, UserRequest } from "./types";
import { CodeeConfig } from "../config/types";
export declare class CodeeEngine extends EventEmitter implements ICodeeEngine {
    private static instance?;
    private readonly sessionManager;
    private readonly contextBuilder;
    private readonly modelRouter;
    private readonly languageRegistry;
    private readonly codeGenerator;
    private readonly codeValidator;
    private readonly conversationStore;
    private readonly projectIndex;
    private readonly vectorMemory;
    private readonly embeddingProvider;
    private readonly contextAssembler;
    private readonly performance;
    private settingsManager?;
    private activeConfig?;
    private activeProjectPath?;
    private indexAbortController?;
    private telemetryEnabled;
    private developerMode;
    private maxIndexFiles;
    private indexingPromise?;
    private indexingTimer?;
    private indexingIdleDelayMs;
    private indexingInProgress;
    private pendingPriorityPaths;
    private languageServerCleanupTimer?;
    private languageServerIdleMs;
    private constructor();
    static getInstance(): CodeeEngine;
    initialize(options?: {
        projectPath?: string;
    }): Promise<void>;
    createSession(projectPath?: string): Promise<Session>;
    processRequest(request: UserRequest): Promise<StreamResponse>;
    switchModel(modelId: string): Promise<void>;
    applyRuntimeOverrides(overrides: Partial<CodeeConfig>): Promise<void>;
    clearLocalData(): Promise<void>;
    startIndexingNow(projectPath?: string): Promise<void>;
    getPerformanceMetrics(): Array<import("../perf/PerformanceMonitor").PerformanceMetric>;
    enableTelemetry(enabled: boolean): void;
    private handleRequest;
    private emitEvent;
    private reloadConfig;
    private validateConfig;
    private prepareProject;
    private scheduleIndexing;
    private ensureIndexing;
    private startIndexing;
    private runIndexing;
    private indexPriorityFiles;
    warmLanguageServer(projectPath: string, languageId: string): Promise<void>;
    private collectDiagnostics;
    private scheduleLanguageServerCleanup;
    private handleError;
    log(entry: EngineLogEntry): void;
    track(event: EngineTelemetryEvent): void;
}
